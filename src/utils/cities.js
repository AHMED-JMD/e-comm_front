/**
 * Cities the platform delivers to. Shared by checkout and the profile page so
 * the saved shipping address always matches what checkout offers.
 */
export const DELIVERY_CITIES = ["الخرطوم", "أم درمان", "بحري"];
